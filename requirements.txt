# 🦞↔🦞 Claw-to-Claw 龙虾间通信
httpx>=0.27.0           # 异步 HTTP 客户端（发消息给其他龙虾）
aiohttp>=3.9.0          # 终端模式内嵌 C2C HTTP 服务器
websockets>=12.0        # Relay Server & Client（零配置龙虾互联）

# 配置与数据
pyyaml>=6.0
pydantic>=2.0.0

# 工具
python-dateutil>=2.8.0
loguru>=0.7.0

# 安全 — E2E 端到端加密 (v1.4)
cryptography>=42.0.0

# AI 扩展（可选，pip install weclaw[ai]）
# openai>=1.0.0
# tenacity>=8.2.0
